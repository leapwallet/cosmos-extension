{
  "name": "@leapwallet/types",
  "version": "0.0.5",
  "description": "Types for leap provider",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc tsconfig.json -w",
    "eslint:lint": "eslint ./src",
    "eslint:fix": "eslint --fix ./src",
    "prettier:check": "prettier -c ./src",
    "prettier:write": "prettier -w ./src",
    "prepublish": "yarn build"
  },
  "main": "dist/index.js",
  "devDependencies": {
    "typescript": "5.2.2"
  }
}
